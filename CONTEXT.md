# Halo Studio

Halo Studio 是面向本地开发者的工作台；受管任务在受信任 Git 工作区中通过 BitFun Runtime 运行内置 Code Agent、Pi 与 OpenCode。

## 文档权威

**权威文档**：
描述 Halo Studio 当前产品边界、已生效决策或待实施工作的唯一有效依据；发生冲突时，历史资料不得覆盖它。
_避免使用_：当前文档、最新文档、有效资料

**历史基线**：
为行为等价迁移、审计或复现保留的旧产品证据；它可以说明过去实现过什么，但不能指导当前产品实现。
_避免使用_：旧需求、当前设计、兼容要求

**待删除文档**：
没有独立审计价值的重复副本、临时计划或已被权威文档完整替代的资料；确认引用和证据需求后从产品树移除。
_避免使用_：历史基线、所有过时文档

## 受管执行器

**受管执行器**：
由 BitFun Runtime 统一调度、被 Halo Studio 明确选择并观察的内置 Code Agent、Pi 或 OpenCode 执行主体。
_避免使用_：任意 Agent、未登记的第三方执行器

## 编码交付

**本地开发者**：
在自己的本地 Git 工作区中使用 Halo Studio、并亲自审查受管交付结论的个人开发者。
_避免使用_：团队协作者、办公用户

**可验证编码交付**：
本地开发者以任务基线、Git 变更、验证结果、人工审查和显式交接为依据，接受或拒绝受管执行器完成的有限编码任务。
_避免使用_：通用 AI 服务、自动交付、完成消息

**可迁移能力基线**：
在待替换产品基座上已实现并经自动化验证、可作为迁移输入的行为与契约；它不构成目标产品基座的产品验收或发布放行。
_避免使用_：最终产品验收、发布完成、P0 通过

**行为等价迁移**：
在新产品基座上保持可迁移能力基线的用户可观察语义，但不承诺兼容被替代实现的内部模块或传输协议。
_避免使用_：逐文件搬运、旧 IPC 兼容层、双运行时桥接

**Agent 任务**：
本地开发者在本地 Git 工作区中显式创建，并指定任务模式与主执行器的有限编码请求；受管交付模式要求工作区可信，多个受管任务可以并发运行，但其写入必须受文件写入租约约束。
_避免使用_：自由聊天、自动委派任务

**受管任务会话**：
围绕一个受管任务、由 BitFun Runtime 启动并持有的有限真实执行器会话；它支持初始任务消息和同一任务范围内的后续澄清或追问，并由本地开发者显式结束后进入交付审查。
_避免使用_：无限制自由聊天、跨任务会话、通用聊天会话

**活动会话记录**：
当前受管任务会话中面向本地开发者显示的用户消息和经脱敏、限长整理后的 Agent 回复；它不含原始工具日志，且不作为完整聊天记录持久化。
_避免使用_：完整聊天档案、原始终端输出、交付历史

**标准会话历史**：
标准编码模式沿用 BitFun 持久化的可重开会话历史；它不构成 Halo Studio 的受管交付证据或审查结论。
_避免使用_：受管会话档案、交付历史、审查证据

**等待开发者**：
受管任务会话在一次主执行器回复后仍可接收后续消息的非终态；此时不固定交付证据，也不进入审查。
_避免使用_：任务完成、运行时空闲、自动结束

**任务说明**：
本地开发者显式提供的任务目标，以及主动选取的文件、已有 Diff 或补充说明；它不自动附带完整工作区或历史。
_避免使用_：隐式完整工作区上下文、自动附带完整历史

**任务基线**：
创建受管任务时记录的 Git 状态与已有变更，用于识别任务开始后的关联变更，同时保留用户已有修改；任务期间发生人工编辑时，不断言关联变更全部由主执行器编写。
_避免使用_：必须干净的工作树、自动暂存

**主执行器**：
用户为某个 Agent 任务显式选择、对当前交付负责的受管执行器；它可以在用户确认的交接后被替换。
_避免使用_：主 Agent、自动分配执行器、默认执行器

**原生工作区改动**：
主执行器按自身原生权限模型直接在工作区产生的文件变更；Halo Studio 只对受管任务中的变更进行归因和审查。
_避免使用_：Halo 受管写入、统一补丁应用

**已接受交付**：
本地开发者接受的最新可审查交付；它只记录任务结论，不创建 Git 提交、分支、拉取请求或发布。
_避免使用_：自动提交、自动发布

**已拒绝交付**：
本地开发者拒绝但仍保留任务记录和关联工作区改动的可审查交付。
_避免使用_：自动回滚、删除任务

**可审查交付**：
受管任务的主执行器已结束、并包含可供本地开发者审查的变更与验证结果的交付证据。
_避免使用_：运行中的交付、完成提示

**交付证据版本**：
一次运行、重试或主执行器交接产生的追加式交付证据；只有最新且新鲜的可审查交付可以得到任务当前结论。
_避免使用_：覆盖旧结果、接受过期交付

**证据新鲜度**：
交付证据覆盖文件的快照是否仍与当前工作区一致的事实；任一覆盖文件被后续改动时，证据标为过期，原快照仍可查看但必须重新审查后才能接受。
_避免使用_：全工作区必须静止、忽略后续改动、过期证据接受

**任务取消**：
本地开发者发起的任务终止流程：由 BitFun Runtime 协调主执行器的原生停止并记录最终取消方式。
_避免使用_：静默杀进程、无结论中断

**任务中断**：
受管任务会话在应用或运行时意外退出前未进入终态的如实状态；它不会自动恢复或重放，后续处置由本地开发者明确决定。
_避免使用_：自动续跑、自动重连、静默失败

**Agent 操作请求**：
受管执行器暂停任务、等待本地开发者在当前受管任务会话中作出一次性权限、澄清或高风险外部操作决定的请求；它不创建会话级或永久放行规则。
_避免使用_：通用批准对话框、隐式授权、持久授权

**交付历史**：
本地持久化的 Agent 任务、交付证据、交接包和接受结论；默认只保存经过脱敏和大小限制的摘要与 Diff 证据，不做云同步或远程共享。
_避免使用_：完整对话档案、原始工具日志、凭据归档、云端任务库

**交付审查**：
本地开发者以只读文件变更与 Diff、验证结果和任务证据判断是否接受、拒绝或交接一份可审查交付的过程。
_避免使用_：Halo 内置编辑、盲目接受结果

## 工作台体验

**开发工作台**：
围绕活动工作区、Agent 任务、交付证据和文件变更组织的 Halo Studio 主界面。
_避免使用_：聊天优先助手、通用终端壳

**受管 TUI 会话**：
用户在当前受信任 Git 工作区中显式开启、仅面向 Pi 或 OpenCode 的交互式原生命令会话；它不携带 Halo Studio 保存的凭据。
_避免使用_：任意 Shell、通用 PTY、凭据透传终端

**扩展能力**：
MCP、插件、Skills、Prompts、扩展市场和自动发现等 BitFun 生态集成；首期只在标准编码模式可用，不进入受管交付模式。
_避免使用_：受管任务自动扩展、伪统一能力、未经审核的受管工具

## 发布范围

**Windows 首发**：
Halo Studio 的首个发布与验收目标平台；路径、进程、凭据和 IPC 边界保持可移植，但 macOS/Linux 发布不阻塞当前版本。
_避免使用_：首期全平台发布、Windows 专属产品定义

**原生开发工作台**：
面向本地开发者的桌面工作台，围绕受管任务、交付证据审查和人工交接组织编码交付体验。
_避免使用_：通用聊天助手、无边界的 Agent 平台

**BitFun 下游产品基座**：
Halo Studio 持续获取但不直接修改或发布到 BitFun 上游的独立产品基础；上游变更只有经过显式同步与 Halo 验证门槛后才进入产品树。
_避免使用_：一次性快照、只读参考项目、直接向上游提交

**上游参考树**：
位于 Halo Studio 工作区之外、用于获取和检查 BitFun 上游变更的干净源码树；它不是 Halo 产品构建依赖，也不是 Halo 功能的提交位置。
_避免使用_：产品源码树、运行时依赖、可修改的 Halo 模块

**Halo 产品树**：
Halo 仓库中受跟踪的 `product/` 产品源码边界，完整承载可持续同步的 BitFun 下游源码与 Halo 差异化能力；迁移完成后它是唯一正式桌面构建入口。
_避免使用_：外部 BitFun 目录、临时 vendor、旧 Halo 产品入口

**本地编码主链**：
首期在桌面端暴露的工作区、Agent 会话、编码工具、交付审查与配置闭环；办公协作、Mini App、远程连接、Relay 与跨端能力既不进入导航，也不在启动时初始化。
_避免使用_：完整 Agent 平台、全端产品套件、未实现功能入口

**受管编码运行时**：
Halo Studio 中唯一管理编码会话、工具执行与运行事实的运行时；它以 BitFun Runtime 为基础承载受管任务、交付证据与人工决策，不存在并行权威会话。
_避免使用_：双运行时、仅界面桥接

**受管交付模式**：
由本地开发者在创建编码任务时明确选择的模式；它要求受信任工作区、生成可审查的交付证据，并以人工决策结束交付。
_避免使用_：自动受管、隐式审查

**受管工具集**：
首期受管交付模式允许内置 Code Agent、Pi 与 OpenCode 使用全部 BitFun 本地工具能力，包括浏览器与系统级 Computer Use；它不包含 MCP、Skills、插件或自定义 Agent。
_避免使用_：自动加载扩展、通用扩展平台、任意第三方工具

**高风险外部操作**：
浏览器或系统级 Computer Use 对工作区之外产生写入、提交、上传、下载、剪贴板写入、进程或系统控制影响的操作；每次均须经 Agent 操作请求得到开发者的一次性决定。
_避免使用_：只读浏览、截图观察、会话级授权、永久放行

**标准编码模式**：
继承 BitFun 本地编码工作流但不产生 Halo Studio 受管交付结论的编码任务模式。
_避免使用_：未受管模式、低权限模式

**受管任务接入**：
将标准编码模式中的当前工作区状态作为新受管任务的基线的显式操作；原会话与此前变更保持独立，只有接入后的改动获得受管归因与交付证据。
_避免使用_：Agent 交接、追溯转换、历史归因

**文件写入租约**：
当主执行器的原生权限策略允许写入时，受管编码运行时自动授予活动受管任务对单个工作区文件的临时独占写入权；读取可并发，冲突任务只能等待、改用其他文件或取消。
_避免使用_：逐文件 Halo 审批、预先锁定工作集、自动合并、后写者覆盖

**人工介入**：
本地开发者对已租用文件进行写入时优先取得该文件的控制权，持有任务暂停，相关文件的任务归因标为混合；开发者随后显式恢复任务或释放文件。
_避免使用_：静默并写、自动恢复、纯 Agent 归因

**运行轨迹**：
任务运行时由 BitFun Runtime 规范化、按序发送给界面的瞬时任务阶段、Agent 操作请求和验证状态；它是主界面的结构化过程视图，不是默认持久化的原始会话记录。
_避免使用_：原始终端输出、永久会话档案

**验证结果**：
由受管执行器原生运行时产生，或由本地开发者明确标记为未执行的通过、失败或未执行结论；Halo Studio 不自行运行任意验证命令。
_避免使用_：Halo 自动测试、隐式验证

**真实会话验收**：
本地开发者在验收工作区通过 Halo Studio 原生界面完成真实受管任务会话的验收；它补充自动化测试，并且只记录脱敏后的结果和交付证据。
_避免使用_：模拟进程验收、持续集成在线测试、完整对话存档

**Agent 交接**：
本地开发者将已审阅交付的有限交接包明确转交给另一受管执行器继续或审查同一 Agent 任务的行为。
_避免使用_：自动委派、自动故障转移、运行中转交

**交接包**：
经用户预览的有限上下文，包含任务目标、主执行器摘要、选定文件变更和验证结果，默认排除完整对话、原始工具日志、凭据和配置文件。
_避免使用_：全量会话转移、自动共享上下文

## 工作区信任

**受信任 Git 工作区**：
用户显式打开、经真实路径校验并确认可信的 Git 仓库；它是受管任务、受管项目配置加载和受管运行时启动的必要项目上下文。标准编码模式可在未取得该信任状态的 Git 工作区中依照 BitFun 的本地权限模型运行。
_避免使用_：自动信任、将信任作为标准模式的全局开关

**验收工作区**：
本地开发者为验证真实受管任务会话而明确选择的、可安全丢弃的受信任 Git 仓库；它不等同于正在开发的项目工作区。
_避免使用_：生产项目、隐式测试目录

**活动工作区**：
当前唯一承载 Halo Studio 任务与受管运行时的受信任 Git 工作区；切换、撤销信任或退出会使其运行时停止并清理。
_避免使用_：多个并行工作区、全局当前路径

**受管启动配置**：
由 BitFun Runtime 解析的模型、思考级别、凭据引用和执行器启动选项集合；凭据明文只在实际认证或启动时短暂使用。
_避免使用_：持久化明文凭据、日志密钥、明文配置回退

**凭据引用**：
产品配置中指向操作系统凭据存储条目的名称；它可以被界面和 IPC 传递，但本身不包含凭据明文。模型、执行器、配置备份、IPC、日志和交付历史只能保存该引用，系统存储不可用时必须失败关闭。
_避免使用_：API 密钥、明文凭据、配置文件密钥、备份密钥

**凭据录入**：
将开发者提供的密钥一次性写入操作系统凭据存储并返回凭据引用的隔离流程；普通模型设置、前端状态、配置、历史和日志不会保留密钥值。
_避免使用_：普通设置表单密钥字段、持久化输入值、日志回显

**诊断日志**：
用于本地故障排查的运行信息；所有内容与导出都经过统一脱敏，不记录密钥、模型请求或响应的明文，也不存在可临时开启的敏感诊断模式。
_避免使用_：敏感诊断、原始模型追踪、未脱敏日志导出

**运行时可用性**：
某个受管执行器经过真实探测、启动和就绪检查得出的独立健康状态；它不代表另一受管执行器或整个工作台可用。
_避免使用_：静态在线状态、模拟就绪、全局在线状态

**兼容性档案**：
某一外部受管执行器版本家族经验证可提供的协议能力与运行语义集合；运行时必须通过其能力检查才可用，新的主版本须先建立新档案。
_避免使用_：只看版本号、无条件兼容、静默放行

**配置事务**：
对 Pi 或 OpenCode 原生配置文件的独立受管变更，包含 Diff 预览、用户确认、冲突检测、原子写入与可验证回滚。
_避免使用_：静默覆盖、Agent 任务写入、全文件盲写
