# Halo Studio

Halo Studio 是面向本地开发者的工作台，在受信任 Git 工作区中受管 Pi 与 OpenCode 两个编码应用。

## 受管应用

**受管应用**：
由 Halo Studio 明确发现、配置、启动和观察的 Pi 或 OpenCode 运行时。
_避免使用_：通用 Agent、第三方 Agent、Claude Code、Codex

**受管运行时**：
受信任工作区边界内由 Rust Sidecar 管理其生命周期的 Pi 或 OpenCode 进程或服务。
_避免使用_：QML 进程、浏览器回退服务、静态在线状态

## 编码交付

**本地开发者**：
在自己的受信任 Git 工作区中使用 Halo Studio、并亲自审查交付结论的个人开发者。
_避免使用_：团队协作者、办公用户

**可验证编码交付**：
本地开发者以任务基线、Git 变更、验证结果、人工审查和显式交接为依据，接受或拒绝 Pi 或 OpenCode 完成的有限编码任务。
_避免使用_：通用 AI 服务、自动交付、完成消息

**Agent 任务**：
本地开发者在单一受信任 Git 工作区中显式创建，并指定 Pi 或 OpenCode 作为主 Agent 的有限编码请求；首期一个活动工作区同一时刻只运行一个任务。
_避免使用_：自由聊天、自动委派任务

**任务说明**：
本地开发者显式提供的任务目标，以及主动选取的文件、已有 Diff 或补充说明；它不自动附带完整工作区或历史。
_避免使用_：隐式完整工作区上下文、自动附带完整历史

**任务基线**：
创建 Agent 任务时记录的 Git 状态与已有变更，用于识别任务开始后的关联变更，同时保留用户已有修改；任务期间发生人工编辑时，不断言关联变更全部由 Agent 编写。
_避免使用_：必须干净的工作树、自动暂存

**主 Agent**：
用户为某个 Agent 任务显式选择、对当前交付负责的受管应用；它可以在用户确认的交接后被替换。
_避免使用_：自动分配 Agent、默认 Agent

**原生工作区改动**：
主 Agent 按自身原生权限模型直接在受信任 Git 工作区产生的文件变更；Halo Studio 只观察、归因和审查这些变更。
_避免使用_：Halo 受管写入、统一补丁应用

**交付证据**：
将 Agent 任务与其摘要、文件变更和验证结果关联起来，供本地开发者审查、接受、拒绝或交接的记录。
_避免使用_：完成消息、聊天记录

**已接受交付**：
本地开发者接受的最新可审查交付；它只记录任务结论，不创建 Git 提交、分支、拉取请求或发布。
_避免使用_：自动提交、自动发布

**已拒绝交付**：
本地开发者拒绝但仍保留任务记录和关联工作区改动的可审查交付。
_避免使用_：自动回滚、删除任务

**可审查交付**：
主 Agent 已结束、并包含可供本地开发者审查的变更与验证结果的交付证据。
_避免使用_：运行中的交付、完成提示

**交付证据版本**：
一次运行、重试或 Agent 交接产生的追加式交付证据；只有最新的可审查交付可以得到任务当前结论。
_避免使用_：覆盖旧结果、接受过期交付

**任务取消**：
本地开发者发起的任务终止流程：先请求受管应用原生停止，超时未退出时由 Rust Sidecar 强制终止，并记录最终取消方式。
_避免使用_：静默杀进程、无结论中断

**Agent 操作请求**：
Pi 或 OpenCode 暂停任务、等待本地开发者通过其原生通道作出权限或澄清决定的请求。
_避免使用_：通用批准对话框、隐式授权

**交付历史**：
本地持久化的 Agent 任务、交付证据、交接包和接受结论；默认只保存经过脱敏和大小限制的摘要与 Diff 证据，不做云同步或远程共享。
_避免使用_：完整对话档案、原始工具日志、凭据归档、云端任务库

**持久化交付证据**：
交付历史中保存的、经过脱敏和大小限制的 Diff 与结果摘要；完整原始 Diff 只从当前 Git 工作区按需读取，无法安全保留时必须明确标记。
_避免使用_：原始 Diff 档案、静默截断

**交付审查**：
本地开发者以只读文件变更与 Diff、验证结果和任务证据判断是否接受、拒绝或交接一份可审查交付的过程。
_避免使用_：Halo 内置编辑、盲目接受结果

## 工作台体验

**开发工作台**：
围绕活动工作区、Agent 任务、交付证据和文件变更组织的 Halo Studio 主界面。
_避免使用_：聊天优先助手、通用终端壳

**受管 TUI 会话**：
用户在当前受信任 Git 工作区中显式开启、仅面向 Pi 或 OpenCode 的交互式原生命令会话；它不携带 Halo Studio 保存的凭据。
_避免使用_：任意 Shell、通用 PTY、凭据透传终端

**扩展能力**：
MCP、插件、Skills、Prompts、扩展市场和自动发现等生态集成；它们不属于首个可验证编码交付版本，并须分别遵循 Pi 或 OpenCode 的原生语义。
_避免使用_：首期通用扩展平台、伪统一能力

## 发布范围

**Windows 首发**：
Halo Studio 的首个发布与验收目标平台；路径、进程、凭据和 IPC 边界保持可移植，但 macOS/Linux 发布不阻塞当前版本。
_避免使用_：首期全平台发布、Windows 专属产品定义

**原生开发工作台**：
以原生桌面界面承载开发工作台体验，并将受管运行时与界面进程隔离的 Halo Studio 产品形态。
_避免使用_：浏览器渲染工作台、WebView 产品外壳

**运行轨迹**：
任务运行时由 Rust Sidecar 规范化、按序发送给界面的瞬时任务阶段、Agent 操作请求和验证状态；它是主界面的结构化过程视图，不是默认持久化的原始会话记录。
_避免使用_：原始终端输出、永久会话档案

**验证结果**：
由 Pi 或 OpenCode 原生运行时产生，或由本地开发者明确标记为未执行的通过、失败或未执行结论；Halo Studio 不自行运行任意验证命令。
_避免使用_：Halo 自动测试、隐式验证

**Agent 交接**：
本地开发者将已审阅交付的有限交接包明确转交给另一受管应用继续或审查同一 Agent 任务的行为。
_避免使用_：自动委派、自动故障转移、运行中转交

**交接包**：
经用户预览的有限上下文，包含任务目标、主 Agent 摘要、选定文件变更和验证结果，默认排除完整对话、原始工具日志、凭据和配置文件。
_避免使用_：全量会话转移、自动共享上下文

## 工作区信任

**受信任 Git 工作区**：
用户显式打开、经真实路径校验并确认可信的 Git 仓库；它是 Agent 任务、项目配置加载和受管运行时启动的唯一项目上下文。
_避免使用_：任意目录、未确认项目

**活动工作区**：
当前唯一承载 Halo Studio 任务与受管运行时的受信任 Git 工作区；切换、撤销信任或退出会使其运行时停止并清理。
_避免使用_：多个并行工作区、全局当前路径

**受管启动配置**：
由 Halo Studio 后台主进程解析的模型、思考级别、Provider 凭据引用和启动选项集合；凭据明文只在启动受管应用时短暂使用。
_避免使用_：界面明文凭据、日志密钥、明文配置回退

**运行时可用性**：
Pi 或 OpenCode 经过真实探测、启动和就绪检查得出的独立健康状态；它不代表另一受管应用或整个工作台可用。
_避免使用_：静态在线状态、模拟就绪、全局在线状态

**配置事务**：
对 Pi 或 OpenCode 原生配置文件的独立受管变更，包含 Diff 预览、用户确认、冲突检测、原子写入与可验证回滚。
_避免使用_：静默覆盖、Agent 任务写入、全文件盲写
