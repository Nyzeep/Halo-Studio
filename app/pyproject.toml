[project]
name = "halo-studio"
version = "0.1.0"
description = "Halo Studio —— Pi/OpenCode 可验证编码交付原生工作台（PySide6/QML）"
requires-python = ">=3.13"
dependencies = ["PySide6>=6.11"]

[project.optional-dependencies]
dev = ["pytest", "pytest-qt"]

[tool.pytest.ini_options]
testpaths = ["tests"]
