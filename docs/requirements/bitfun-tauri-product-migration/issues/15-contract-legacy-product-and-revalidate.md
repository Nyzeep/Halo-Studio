# 15 - 收缩旧产品实现并完整复验

**What to build:** 在新 Tauri 产品通过全部迁移门槛后，维护者可以通过独立变更删除旧 QML、旧 Sidecar 和旧启动入口，使 Halo 只剩一个正式桌面产品与一个权威运行时，并在最终仓库状态重新获得发布证据。

**Blocked by:** 14 - 完成真实 OpenCode 原生 UI 验收.

**Status:** ready-for-agent

- [ ] 删除范围由引用审计和行为等价矩阵确定，只移除已被新产品替代的旧入口、适配层、传输边界、测试与文档。
- [ ] 可复用的纯领域逻辑和测试夹具已迁入新运行时后再删除旧宿主，不保留长期双运行时桥接。
- [ ] 仓库、脚本、文档和发布配置只指向 Halo Tauri 产品，不再提供旧 PySide/QML 或 Sidecar 产品入口。
- [ ] 删除后重跑完整 Tauri 构建与打包、Rust/前端契约、桌面端到端、行为等价矩阵、同步演练和许可证检查。
- [ ] 删除后再次完成真实 OpenCode 原生 UI 主链与中断验收；任何失败都会阻止 P0 放行。
