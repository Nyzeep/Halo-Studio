---
status: accepted
---

# 将长期 Halo 产品源码置于 product 边界

Halo Studio 将完整 BitFun 下游源码和 Halo 差异化实现维护在仓库受跟踪的 `product/` 产品树中，而不与现有根目录旧实现直接合并或在迁移后再次搬回根目录。该边界避免独立历史和同名根路径冲突，使上游同步、迁移期并存和最终删除旧源码都保持可审查；迁移完成后 `product/` 成为唯一正式桌面构建入口。
