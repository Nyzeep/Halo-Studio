---
status: accepted
---

# 以文件写入租约支持并发受管任务

Halo Studio 允许同一受信任工作区中存在多个活动受管任务，但由 BitFun Runtime 在首次写入时为任务分配文件级独占写入租约。读取保持并发；遇到已被占用的文件时，任务暂停并等待开发者选择等待、改用其他文件或取消，不自动合并或覆盖。该决定以显式协调换取多任务下仍可信的归因、证据和审查结论。
