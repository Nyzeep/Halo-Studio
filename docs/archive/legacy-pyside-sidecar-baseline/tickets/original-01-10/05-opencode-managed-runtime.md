# 05 - OpenCode 受管运行时

**构建目标：** 让受信任工作区中的 OpenCode 由 Halo Studio 管理的锁定版本本地服务启动，并以回环认证、健康检查和精确版本握手确认其真实可用性。

**依赖：** 02 - 可信单工作区生命周期；03 - 受管启动配置与凭据隔离。

**状态：** 发布资格阻塞：受控服务自动化通过，但本机 OpenCode `1.18.5` 与锁定版本 `0.4.2` 不匹配；需确认目标锁定版本后再验证真实服务。

- [x] OpenCode 服务只绑定回环地址，并在每次启动使用新的认证信息。
- [ ] 真实安装版健康检查和精确版本握手未通过资格验证：当前版本 `1.18.5` 不等于锁定 `0.4.2`。
- [x] 健康失败、版本不匹配、服务退出和停止均具有明确恢复建议和清理行为。
- [x] 受控 OpenCode 服务集成测试覆盖认证、健康状态、版本失败和优雅停止。

**验收与 TDD 证据：** `docs/requirements-alignment/03-original-ten-task-acceptance-and-tdd-baseline.md`、`docs/traceability.md`。
