# 需求追踪（任务 01–10 → 代码与测试）

> **历史基线记录：** 本文追踪旧 PySide/QML + Sidecar 实现及其 OpenCode/Pi 资格证据，只用于迁移审计和行为等价比较；它不定义当前 Pi RPC P0 的实现、测试或发布门槛。当前门槛以 `docs/requirements/bitfun-tauri-product-migration/` 下的活动工单为准。

> 由验收阶段填写并维护。每项列出实现位置与自动化验证位置（文件级）。
>
> 验收核查日期：2026-07-27（原始 01–10 任务复核与 TDD 补测）。完整结论见
> `docs/verification/migratable-capability-baseline/original-ten-task-acceptance-and-tdd-baseline.md`。
> 本轮实测：`scripts\test-all.ps1` 通过（Rust workspace 252 例，0 失败；Python
> 63 通过、1 条设计内条件跳过）；`scripts\smoke-windows.ps1` 退出码 0（默认平台
> `SMOKE-OK`，根入口、app、sidecar 与 QML 全部静态红线 PASS）。
> 真实运行时资格不由受控假进程替代：Pi 未安装；本机 OpenCode `1.18.5` 属于当前稳定
> `>= 1.18.5, < 2.0.0` 兼容性档案，已在私有 XDG 环境实测 `serve` 的回环绑定、每次启动
> Basic 认证、`/global/health`、`/global/dispose` 和进程清理。完整 Sidecar 二进制端到端
> 验证受本机 MSVC 链接工具链阻断；Credential Manager 正向注入仍需要可写的交互式会话。

| 任务 | 验收标准 | 实现 | 验证 |
| --- | --- | --- | --- |
| 01 原生工作台最小闭环 | Windows 可启动原生应用，显示 Sidecar 连接、协议版本、不可用原因 | `app/halo_studio/main.py`（--smoke）、`app/halo_studio/app.py`、`app/halo_studio/viewmodels/app_vm.py`、`app/halo_studio/qml/Main.qml`（底部状态条常显三项） | `app/tests/test_smoke.py::test_smoke_main_ok`、`app/tests/test_viewmodels.py::TestAppViewModel`（test_sidecar_ready_sets_three_properties / test_disconnect_reason_passthrough）、`scripts/smoke-windows.ps1`（实测 PASS） |
| 01 | 请求、事件、错误均经版本化 JSONL 契约验证 | `sidecar/crates/halo-protocol/src/`（envelope.rs / io.rs / error.rs / methods/*）、`app/halo_studio/ipc/connection.py`、`app/halo_studio/ipc/client.py` | `sidecar/crates/halo-protocol/tests/contract.rs`（32 例：roundtrip、错误码稳定性、超长行/坏 JSON/错误版本拒绝）、`app/tests/test_connection.py`（hello 协商、坏 JSON、超长行、乱序配对、seq 单调、缺失必填字段和未知字段拒绝） |
| 01 | 生产路径无 Electron/WebView 外壳、无 Mock Agent 回退 | `app/pyproject.toml`（仅 PySide6）、`app/halo_studio/qml/**`（纯 QML）；生产代码经检索无 mock/模拟回退 | `scripts/smoke-windows.ps1`（静态断言：pyproject/Cargo.toml 无 electron/react/vite/webview、QML 无 WebEngine/WebView、无 package.json）、`app/tests/test_smoke.py::test_qml_static_no_web_components` |
| 01 | 原生应用基础、Sidecar 契约、Windows 烟测均有自动化验证 | 同上 | `cargo test --workspace`（252 例全绿）+ `pytest tests`（63 通过、1 条条件跳过）+ `scripts/smoke-windows.ps1`（退出码 0） |
| 02 可信单工作区生命周期 | 未信任工作区不能加载 Agent 配置、插件或启动运行时 | `sidecar/crates/halo-sidecar/src/dispatch.rs`（runtime_start / task_create 的 WORKSPACE_NOT_TRUSTED 守卫）、`sidecar/crates/halo-core/src/trust.rs` | `sidecar/crates/halo-integration-tests/tests/workspace_boundary.rs::untrusted_workspace_rejects_runtime_and_task`、`halo-sidecar/src/dispatch.rs::tests::workspace_open_trust_and_untrusted_task_create_flow` |
| 02 | 同一时刻一个活动工作区，切换不遗留旧进程 | `dispatch.rs`（workspace_open/close/trust 中 stop_all_runtimes）、`sidecar/crates/halo-sidecar/src/state.rs`（stop_slot） | `workspace_boundary.rs::switching_workspace_stops_old_runtime_process`、`workspace_boundary.rs::revoke_trust_stops_runtime_immediately`（均以 fake-pi 独占锁文件释放确认真实子进程退出） |
| 02 | 空格/CJK 路径、目录替换、不可读目录均有明确用户可见结果 | `sidecar/crates/halo-sidecar/src/git.rs`（validate_workspace：PathInvalid/NotReadable/NotGit + 中文文案）、`halo-core/src/trust.rs`（identity_changed 降级） | `git.rs::tests`（validate_* 系列，路径含空格与中文）、`halo-core/src/trust.rs::tests`（root_commit_mismatch_downgrades_trust 等 7 例）、`workspace_boundary.rs`（非 Git/不存在路径错误码）、`app/tests/test_viewmodels.py::test_workspace_changed_event_and_identity_changed`、`workspace_identity_change.rs::rebuilt_repo_downgrades_trust_with_identity_changed`（目录替换端到端：root_commit 变化 → untrusted + identity_changed=true + runtime.start 被拒）、`workspace_not_readable.rs::unreadable_directory_maps_to_workspace_not_readable`（icacls deny(RD) 构造不可读目录 → WORKSPACE_NOT_READABLE） |
| 02 | 工作区状态经契约提供，生命周期边界有集成测试 | `halo-protocol/src/methods/workspace.rs`、`dispatch.rs`（workspace.*） | `contract.rs::workspace_types_roundtrip / workspace_status_result_untagged_both_ways`、`workspace_boundary.rs` 全部 3 例、`task_running_guard.rs::running_task_blocks_workspace_switch_and_close`（运行中任务 → workspace.open/close 返回 TASK_RUNNING 且工作区不变；accept 终局化后关闭成功）、`test_viewmodels.py::TestWorkspaceViewModel` |
| 03 受管启动配置与凭据隔离 | UI/IPC/日志/Diff/备份/数据库不暴露凭据明文 | `sidecar/crates/halo-config/src/secret.rs`（Secret：无 Display、Debug 恒为 `Secret(***)`）、`halo-store`（仅存 credential_ref）、`halo-sidecar/src/main.rs`（cred CLI stdin 录入不回显） | `sidecar/crates/halo-integration-tests/tests/credential_canary.rs`（canary 全链路：IPC 收发行、HALO_DATA_DIR 全部文件字节、评审 diff、交接包、Agent 产物均无明文）、`secret.rs::tests`、`app/tests/test_viewmodels.py::test_save_whitelists_fields_and_never_holds_secret` |
| 03 | 凭据存储不可用时保存与启动失败关闭，不回退明文 | `dispatch.rs`（config_save / runtime_start 的 CREDENTIAL_STORE_UNAVAILABLE）、`halo-config/src/credential.rs`、`main.rs`（cred set 失败关闭） | `dispatch.rs::tests::config_save_with_credential_fails_closed_when_store_unavailable`、`halo-config/src/credential.rs::tests::unavailable_store_fails_closed_for_all_operations` |
| 03 | 运行时只获得显式白名单环境 | `halo-config/src/launch.rs`（ENV_WHITELIST + build_child_env）、`dispatch.rs::runtime_start`（白名单 + 启动瞬间注入凭据） | `launch.rs::tests`（build_child_env_* 6 例：仅白名单透传、大小写规范、注入恰好一次、拒绝白名单外覆盖）、`dispatch.rs::tests::config_save_rejects_env_outside_whitelist`、`credential_canary.rs`（fake-pi --report-env 证明注入真实发生且只记存在性） |
| 03 | 凭据 canary 与启动配置边界有自动化测试 | 同上 | `credential_canary.rs::credential_canary_never_leaks_across_full_chain`（当前无交互式 Credential Manager 时实测失败关闭且不回显；正向注入链待可写会话资格验证） |
| 04 Pi 受管运行时 | 版本探测 + get_state 就绪检查通过才可用 | `sidecar/crates/halo-runtime/src/pi.rs`（probe + get_state 就绪；ready 前不可用） | `pi.rs::tests::ready_then_run_task_happy_flow / ready_non_idle_state_fails / ready_timeout_fails_and_kills_child`、`happy_pi.rs`（runtime.probe 版本 + runtime.state ready 事件） |
| 04 | EOF、无效协议、异常退出、停止与取消均产生真实可恢复状态 | `pi.rs`（EOF/坏帧 → Failed{reason, recovery_hint}）、`halo-sidecar/src/task_flow.rs`（RuntimeFailed → 任务 failed） | `pi.rs::tests::eof_marks_failed_with_chinese_reason_and_hint / bad_json_frame_marks_failed / stop_graceful_when_child_exits_in_grace / stop_forced_when_child_ignores_grace`、`runtime_failures.rs::pi_not_ready_times_out_with_reason_and_hint / pi_garbage_frames_fail_startup`、`task_flow.rs::tests::runtime_failure_mid_task_marks_failed` |
| 04 | Pi 启动配置不经 UI/公开 IPC 暴露 Provider 凭据 | `halo-protocol/src/methods/config.rs`（LaunchConfig 仅 credential_ref）、`halo-runtime/src/lib.rs`（LaunchCmd Debug 隐藏 env 值） | `halo-runtime` 单测 `launch_cmd_debug_hides_env_values`、`credential_canary.rs`、`contract.rs::launch_config_shape_snapshot` |
| 04 | 受控 Pi 进程集成测试覆盖分帧、乱序、就绪失败、停止 | `sidecar/crates/halo-testkit/src/bin/fake_pi.rs` | `pi.rs::tests::out_of_order_and_unknown_ids_are_tolerated`（内存管道分帧/乱序）、`halo-testkit/tests/fake_pi.rs`（8 例）、`runtime_failures.rs`、`cancel.rs` |
| 05 OpenCode 受管运行时 | 仅回环地址 + 每次启动新认证信息 | `sidecar/crates/halo-runtime/src/opencode.rs`（固定 `127.0.0.1`、空闲端口、每次启动私有 `OPENCODE_SERVER_PASSWORD`） | `happy_opencode.rs`（每次启动生成不同的认证材料摘要）、`halo-testkit/tests/fake_opencode.rs`（假服务只绑定回环并拒绝错误或缺失的 Basic 认证） |
| 05 | `opencode-server-1.x` 档案仅接受稳定 `>= 1.18.5, < 2.0.0`；认证健康检查通过才可用，端口与认证信息不进公开状态 | `opencode.rs`（stdout ready 行绑定确认 + Basic 认证 `GET /global/health`，由健康响应的 version 验证档案；端口和认证材料不出现在公开 getter/Debug） | `opencode.rs::tests`（健康、缺少版本、版本不兼容、认证失败与凭据不泄漏）、`happy_opencode.rs`（RuntimeStateInfo 仅公开契约字段） |
| 05 | 健康失败、版本不兼容、服务退出、停止均有恢复建议与清理 | `opencode.rs`（Failed{reason, recovery_hint}；就绪后监督 server 进程；`POST /global/dispose` 后仍显式结束 server，成功为 Graceful，失败/超时为 Forced） | `runtime_failures.rs`（版本、健康、认证和 `exit_early` 均断言中文 reason + recovery_hint + runtime.state failed 事件）、`opencode.rs::tests`（global dispose 成功回收、失败强制回收） |
| 05 / #11 | 受控 OpenCode 服务集成测试覆盖 Basic 认证、健康、兼容档案、真实首轮 session/message/SSE 回路、停止与无旧假协议回退 | `halo-testkit/src/bin/fake_opencode.rs`、`halo-runtime/src/opencode.rs` | `halo-testkit/tests/fake_opencode.rs`（Basic 401、unhealthy、版本档案、真实 session/SSE、`stale_idle`、`/global/dispose`、hang_on_dispose）、`happy_opencode.rs`（首轮消息、`waiting_developer`、快照和无自动 `task.finished`）、`runtime_failures.rs` |
| 06 单任务与结构化运行轨迹 | 一个活动工作区同一时刻只允许一个运行中任务 | `dispatch.rs::task_create`（has_running_task → TASK_ALREADY_RUNNING）、`state.rs::has_running_task` | `evidence_versions.rs`（运行中再建任务 → TASK_ALREADY_RUNNING 实测） |
| 06 | 任务只接收用户显式提供的说明、文件、Diff、补充信息 | `halo-protocol/src/methods/task.rs`（TaskSpec）、`halo-runtime/src/lib.rs`（RunTaskSpec 四字段）、`dispatch.rs::task_create`（不附带工作区/历史；handoff 上下文仅在显式 handoff_id 时并入） | `contract.rs::task_create_params_shape_snapshot`、`app/tests/test_viewmodels.py::test_create_sends_spec_with_nulls`、`halo-testkit/tests/fake_pi.rs::happy_full_script`（run_task params 形状） |
| 06 | 原生输出统一为有序事件与可恢复快照；主界面非原始终端输出 | `halo-sidecar/src/server.rs`（EventBus：全局 seq + 1024 环形缓冲）、`task_flow.rs`（RuntimeEvent → trace.item/task.phase 规范化 + sanitize + cap）、`app/halo_studio/viewmodels/trace_vm.py`、`app/halo_studio/qml/views/TaskView.qml`（结构化轨迹列表） | `happy_pi.rs`（seq 严格递增、phase 顺序、trace 四类）、`event_recovery.rs::snapshot_after_seq_semantics_and_event_gap`（增量/尾部/EVENT_GAP/环形缓冲 1024）、`server.rs::tests`（seq_starts_at_one、ring_keeps_exactly_last_1024_events）、`task_flow.rs::tests::trace_detail_strings_are_recursively_sanitized_and_capped`（trace.item 的 detail JSON 整树递归脱敏 + TRACE_TEXT_MAX 截断）、`test_viewmodels.py::TestTraceViewModel`（排序去重、快照重建、EVENT_GAP 以 oldest_available_seq 重拉） |
| 06 | 取消先原生停止，超时才强制终止，显示最终状态 | `task_flow.rs`（cancel_native → cancel_grace → 强杀；mode=native/forced）、`main.rs::timeouts_from_env`（可注入超时） | `cancel.rs::cancel_native_when_agent_stops_in_grace / cancel_forced_after_grace_timeout_kills_agent`（独占锁文件确认真实子进程已被终止）、`task_flow.rs::tests::cancel_timeout_forces_kill_and_marks_forced / cancel_native_within_grace_marks_native`、`test_viewmodels.py::test_cancel_and_cancelled_event_mode` |
| 07 任务基线与交付证据 | 任务前已有修改不归因 Agent | `halo-sidecar/src/git.rs`（capture_tree 临时索引 + status_dirty_files）、`task_flow.rs::start_task`（基线记录）、`halo-core/src/attribution.rs` | `happy_pi.rs`（tracked/untracked 脏文件进 baseline_dirty_files 且不进关联变更）、`task_flow.rs::tests::baseline_dirty_files_recorded_and_kept_separate`、`halo-core` 单测 baseline_dirty_file_is_never_agent_attributable、`git.rs::tests::capture_tree_includes_untracked_and_leaves_real_index_alone` |
| 07 | 人工编辑 → mixed 归因而非纯 Agent | `dispatch.rs::task_mark_manual_edit`、`halo-core/src/attribution.rs`（with_manual_edit 不可逆） | `manual_edit.rs::manual_edit_during_task_yields_mixed_attribution`、`halo-core` 单测 agent_only_becomes_mixed_on_manual_edit / mixed_accumulates_reasons_and_never_reverts、`task_flow.rs::tests::manual_edit_attribution_lands_in_evidence_as_mixed` |
| 07 | 重试/交接追加证据版本，不覆盖旧结果 | `halo-store/src/store.rs`（evidence 表主键 (task_id,version) + 禁 UPDATE/DELETE 触发器；append_evidence 只发下一版本）、`halo-core/src/evidence.rs`（EvidenceLog 追加式） | `evidence_versions.rs::handoff_retry_appends_evidence_and_old_version_not_acceptable`（v2 追加后 v1 仍可读、EVIDENCE_NOT_LATEST）、`halo-store/tests/store.rs`（append_evidence_assigns_incrementing_versions_per_task；UPDATE 被触发器拒绝、重复版本 INSERT 被主键拒绝）、`halo-core` 单测 append_never_overwrites_old_versions |
| 07 | 本地历史仅脱敏、限长的摘要与 Diff；无明文凭据/完整对话/无限日志 | `halo-core/src/text.rs`（sanitize/cap）+ `limits.rs`、`halo-store/src/limits.rs`（StoreLimits 防御纵深）、`task_flow.rs::append_evidence`（入库前 sanitize+cap） | `halo-core` sanitize 11 例（sk-/AKIA/Bearer/password=/PEM 等）+ cap 5 例、`halo-store/tests/store.rs::oversized_text_is_truncated_and_flagged / default_limits_match_contract`、`task_flow.rs::tests::evidence_summary_sanitizes_git_error_stderr`（GitError 携带的 git stderr 追加进 summary 后整体再脱敏）、`credential_canary.rs`（数据目录全文件扫描）、`evidence_versions.rs`（history.evidence 不含逐文件 diff 正文） |
| 08 只读审查与交付决定 | 审查界面只读，无编辑/保存能力 | `halo-protocol/src/methods/review.rs`（ReviewBundle 只读形状）、`app/halo_studio/viewmodels/review_vm.py`（无写 API）、`app/halo_studio/qml/components/DiffViewer.qml`（readOnly 恒 true） | `test_viewmodels.py::test_readonly_no_write_api`、`test_smoke.py::test_review_diff_component_is_readonly`、`contract.rs::review_bundle_shape_snapshot` |
| 08 | 接受只记录结论，不提交/推送/建分支/发布 | `dispatch.rs::delivery_decide`（仅写 store 的 tasks/decisions；全程不调用 GitClient）、`git.rs`（客户端无任何修改性 git 命令） | `dispatch.rs::tests::delivery_accept_enforces_latest_version_then_terminalizes`、`happy_pi.rs`（accept → 状态/历史结论）、`delivery_git_invariance.rs::accept_never_touches_git_head_or_worktree`（accept 前后 `git rev-parse HEAD` 与 `git status --porcelain` 完全不变） |
| 08 | 拒绝保留任务记录与工作区文件，不回滚/删除 | `dispatch.rs::delivery_decide`（reject 同路径，仅落 DecisionRecord） | `dispatch.rs::tests::delivery_reject_records_reason_and_keeps_evidence`（拒绝后证据与任务记录保留、原因脱敏）、`delivery_git_invariance.rs::reject_never_touches_git_head_or_worktree`（reject 前后 HEAD 与 status 完全不变，Agent 产物文件仍在磁盘） |
| 08 | 验证结论三态（通过/失败/未执行）；Halo 不自行执行任意命令 | `halo-core`（VerificationStatus 三态 + VerificationSource agent/user_marked）、`dispatch.rs::task_mark_verification`（用户只能标 not_run）、`task_flow.rs`（Agent 未报告时如实 not_run；验证只来自运行时事件） | `dispatch.rs::tests::mark_verification_only_accepts_not_run`、`halo-core` 单测 user_marked_verification_is_not_run_only、`halo-testkit` verify_fail 模式 + `task_flow.rs::tests::failed_outcome_marks_failed_with_evidence`（not_run 兜底）、`happy_pi.rs`（source=agent） |
| 09 手动交接与中断恢复边界 | 交接仅在可审查交付结束后；运行中不可转交 | `dispatch.rs::handoff_source`（is_reviewable 守卫 → TASK_STILL_RUNNING） | `handoff_boundary.rs::handoff_rejected_while_task_running`、`dispatch.rs::tests::handoff_preview_running_task_rejected_and_reviewable_flow_works` |
| 09 | 交接包只含目标/摘要/选定变更/验证结果；排除对话、日志、凭据、配置 | `halo-core/src/handoff.rs`（HandoffDraft 白名单构造，类型上无法夹带）、`dispatch.rs::handoff_preview/create` | `handoff_boundary.rs::handoff_package_is_limited_context_only`（字段集合与契约完全一致；禁带 trace 文本/credential_ref/executable_path 等；白名单外路径静默忽略）、`halo-core` 单测 draft_serialization_contains_only_whitelist_fields / handoff_sanitizes_goal_summary_and_diffs、`credential_canary.rs` |
| 09 / #14 | 重启/崩溃后任务如实标记中断；活动会话清除且不自动恢复、重连、重发或重复写入；已有工作区与证据保留 | `main.rs::serve`（启动时 mark_non_terminal_interrupted）+ `main.rs::shutdown`（EOF 收口、关闭闸门、清除活动路由）+ `task_flow.rs`（迟到事件不得追加证据）、`halo-store::mark_non_terminal_interrupted`、`halo-core`（Interrupted 为终态且可审查） | `interruption.rs`（强杀、EOF、OpenCode 不重连/不重复写入、消息不持久化、工作区与证据保留）、`main.rs` 单元测试（退出清除活动会话）、`credential_canary.rs`、`delivery_git_invariance.rs`、`halo-store/tests/store.rs::mark_non_terminal_interrupted_only_touches_non_terminal_tasks`；真实 UI 清单见 `05-real-opencode-release-acceptance-checklist.md` |
| 09 | 无自动委派、自动重试、自动故障转移 | 代码中无任何自动重试/委派路径；交接接续必须显式 handoff.create + 带 handoff_id 的 task.create | `interruption.rs`（重启零重放事件、运行时不自动拉起、工作区不自动恢复）、`evidence_versions.rs`（接续任务必须显式创建且受单任务约束） |
| 10 端到端安全与发布验证 | 受控进程集成测试覆盖完整任务、取消、失败、审查、交接链路 | `sidecar/crates/halo-integration-tests/tests/`（15 个场景文件 + support/mod.rs：真实 halo-sidecar 子进程 + fake-pi/fake-opencode） | 25 例集成测试全绿：任务、取消、运行时失败、证据版本、人工介入、中断、事件恢复、交接、工作区边界/身份/ACL、凭据 canary 和 Git 不变性 |
| 10 | 凭据 canary 不出现在 UI 状态、IPC、SQLite、日志、Diff、备份、交接包 | `credential_canary.rs`（cred set → 引用 → 全链路） | `credential_canary.rs::credential_canary_never_leaks_across_full_chain` 在可用 Credential Manager 时覆盖 IPC、HALO_DATA_DIR、SQLite、review/handoff 和 Agent 产物；当前环境执行失败关闭与不回显分支。`test_viewmodels.py::test_save_whitelists_fields_and_never_holds_secret` 覆盖 UI 状态。 |
| 10 | Windows 烟测证明不依赖 Electron/React/Vite/浏览器窗口 | `scripts/smoke-windows.ps1`、`app/halo_studio/main.py`（--smoke） | 实测 `smoke-windows.ps1` 退出码 0：默认平台 SMOKE-OK；pyproject/全部 Cargo.toml 无被禁依赖；QML（13 文件）无 WebEngine/WebView；无 package.json |
| 10 | 迁移验收拒绝旧多 Agent、Mock 回退、通用终端、WebView 入口 | `halo-config`（AgentKind 仅 pi/opencode）、生产代码零 mock（测试替身仅在 #[cfg(test)]/tests//app/tests/halo-testkit）、QML 无终端/Web 组件 | `smoke-windows.ps1` 静态断言、`test_smoke.py::test_qml_static_no_web_components`、`halo-config` 单测 agent_kind_parses_contract_values_only、代码检索确认生产路径无 mock/模拟回退（本次验收复核） |

## 已知次要缺口（2026-07-27 安全审查修复轮已全部闭合）

1. ~~`WORKSPACE_NOT_READABLE` 无行为级测试~~ → `halo-integration-tests/tests/workspace_not_readable.rs::unreadable_directory_maps_to_workspace_not_readable`：icacls 对当前用户 deny(RD)（只拒列目录、保留读属性，使 metadata/canonicalize 通过、read_dir 失败，精确落在 NotReadable 分支），测试无论成败经 Drop 守卫恢复 ACL；icacls 不可用的机器上如实跳过（本机实测命中断言路径）。
2. ~~`TASK_RUNNING` 守卫无行为级测试~~ → `halo-integration-tests/tests/task_running_guard.rs::running_task_blocks_workspace_switch_and_close`：慢速 fake-pi（--step-delay-ms 500）保持任务运行，期间 `workspace.open`（另一仓库）与 `workspace.close` 均返回 TASK_RUNNING 且活动工作区不变；review_ready 仍属非终态继续阻断，accept 终局化后关闭成功。
3. ~~accept/reject 后 Git 不变无显式断言~~ → `halo-integration-tests/tests/delivery_git_invariance.rs`（accept 与 reject 两条链路：决定前后 `git rev-parse HEAD` 与 `git status --porcelain` 输出完全一致，Agent 产物文件保留）。

## 发布资格待完成

1. Pi 真实安装版不在本机 PATH；需在已安装 Pi 的 Windows 环境执行版本探测、RPC `get_state`、取消和工作区写入资格验证。
2. 本机 OpenCode `1.18.5` 已完成真实服务协议资格验证：私有 XDG 环境中的 `serve` 仅绑定 `127.0.0.1`，无认证健康检查返回 401，每次新 Basic 认证后的 `/global/health` 返回 200，`/global/dispose` 成功且直接子进程被回收；未使用旧假设协议。完整 Sidecar 二进制端到端验证仍待本机 MSVC 链接器及 `msvcrt.lib` 可用后执行。
3. 当前非交互式 Windows Credential Manager 会话只能证明失败关闭和 canary 不回显。可写会话仍需运行正向注入链并确认凭据不进入 UI、IPC、SQLite、日志、Diff、备份和交接包。

## 后续对齐项

`halo-config` 的配置事务已有本地模块与单元测试，但尚未成为 IPC/UI 工作流。完整编辑器、文件写入和配置写入工作流仍不属于本次原始 01–10 任务验收；后续以新的需求确认记录处理。`task.resolve_action` 已由下列 #12 需求确认记录覆盖。

## #12 一次性 Agent 操作请求

| 验收项 | 实现 | 验证 |
| --- | --- | --- |
| 权限与澄清在当前活动任务会话内以关联任务的卡片展示；内容只来自脱敏限长 IPC | `halo-protocol/src/methods/task.rs`（`TaskActionRequest`）、`halo-sidecar/src/task_flow.rs`（快照与 `task.action_request`）、`app/halo_studio/viewmodels/task_vm.py`、`app/halo_studio/qml/views/TaskView.qml` | `halo-protocol/tests/contract.rs`、`halo-sidecar/src/dispatch.rs` 测试、`app/tests/test_viewmodels.py::TestTaskViewModel`、`app/tests/test_smoke.py::test_task_view_exposes_only_one_time_action_controls` |
| 权限只有本次允许或拒绝；澄清只有回答或拒绝；不创建永久放行规则 | `halo-protocol/src/methods/task.rs`（`ActionDecision`）、`halo-sidecar/src/task_flow.rs`（精确 kind/decision 校验）、`TaskView.qml`（仅“本次允许”“回答”“拒绝”） | `halo-protocol/tests/contract.rs`、`halo-sidecar/src/task_flow.rs` 测试、`app/tests/test_viewmodels.py` 的 allow/reject/answer 行为测试与 QML 静态红线 |
| `awaiting_action` 只在匹配请求的真实 Agent 反馈后转为 `running`、`waiting_developer` 或 `failed`；已确认的精确卡片经 `task.action_resolved` 移除 | `halo-runtime/src/opencode.rs`（asked/replied/rejected 与私有请求映射）、`halo-runtime/src/lib.rs`（`ActionResolved`）、`halo-sidecar/src/task_flow.rs`、`app/halo_studio/viewmodels/task_vm.py` | `halo-runtime/src/opencode.rs` 测试、`halo-sidecar/src/task_flow.rs` 测试、`halo-integration-tests/tests/happy_opencode.rs`、`app/tests/test_viewmodels.py::TestTaskViewModel::test_action_resolved_removes_only_the_matching_submitted_request` |
| 不匹配、重复、拒绝与取消稳定失败，不会批准其他请求 | `halo-sidecar/src/dispatch.rs`、`halo-sidecar/src/task_flow.rs`、`halo-sidecar/src/state.rs`（fail-closed handle） | `halo-sidecar/src/dispatch.rs` 与 `task_flow.rs` 测试、`halo-integration-tests/tests/happy_opencode.rs`、`app/tests/test_viewmodels.py` |
| 会话、端口、认证和开发者回答不进入历史、日志或公开卡片状态 | `halo-runtime/src/opencode.rs`、`halo-sidecar/src/task_flow.rs`、`TaskViewModel`（回答仅作单次 IPC 参数） | `halo-runtime/src/opencode.rs` 测试、`halo-integration-tests/tests/happy_opencode.rs`、`app/tests/test_viewmodels.py` |

## #13 追问、显式结束与交付审查

| 验收项 | 实现 | 验证 |
| --- | --- | --- |
| 仅 `waiting_developer` 可向同一 OpenCode session 追问并展示整理回复 | `task.send_message` 协议、`OpenCodeHandle::send_message` 每轮闸门与私有回复游标、`TaskViewModel`/`TaskView.qml` | core/协议/Sidecar/runtime 单测、ViewModel 与 QML 烟测 |
| 显式结束与取消独立，运行中不能正常结束 | `task.finish` 独立控制通道、`FinishRequested` 状态迁移、OpenCode `TaskDone` 忽略规则 | `task_flow`/`dispatch` 状态门控测试，既有 cancel 测试 |
| 结束后复用任务基线生成 Diff、摘要、验证并进入只读审查 | `task_flow::append_evidence`、`ReviewViewModel` 自动加载、`ShellViewModel.showReview` | 显式结束取证单测、Review ViewModel 行为测试、只读 Diff 红线 |
| 会话不持久化；接受/拒绝不改 Git 或文件 | `ActiveTask.session_messages` 内存边界、`to_record`/EvidenceDraft 无会话字段、既有 `delivery_decide` | 会话 sentinel 不进入 evidence；`delivery_git_invariance.rs` 与只读 ViewModel 测试 |
