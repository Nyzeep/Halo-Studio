{
  "name": "halo-studio",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "node scripts/run-workspace-script.mjs dev",
    "build": "node scripts/run-workspace-script.mjs build",
    "typecheck": "node scripts/run-workspace-script.mjs typecheck",
    "test": "node --test scripts/assert-repository.test.mjs scripts/run-workspace-script.test.mjs && node scripts/run-workspace-script.mjs test",
    "check:repository": "node scripts/assert-repository.mjs",
    "verify": "npm run check:repository && npm run typecheck && npm test && npm run build"
  },
  "engines": {
    "node": ">=20.18",
    "npm": ">=10.8"
  }
}
